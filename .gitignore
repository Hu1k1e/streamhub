node_modules/
.env
macOS/
.DS_Store

# Compose files with real secrets — only push the .example version
hub/docker-compose.yml
streamer/.env

# Private planning documents — never push to public repo
instructions.md
project_specs.md
